%h1 Sections List

%table.table.table-striped.table-bordered.table-condensed
  %thead
    %tr
      %th Header
      %th Body
      %th Type
      %th{:colspan => 3} Actions
     
  %tbody
    - @sections.each do |section|
      %tr
        %td= section.header
        %td= section.body
        %td= section.section_type
        %td= link_to 'Show', section
        %td= link_to 'Edit', edit_section_path(section)
        -#%td= link_to 'Delete', bank, :method => :delete, :data => { :confirm => 'Are you sure?' }

%ul.nav.nav-pills
  %li= link_to 'New Bank', new_section_path, :class => "btn btn-small"
